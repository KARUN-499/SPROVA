name: sprova
description: "Sprova - Build a Startup in 30 Days"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.2
  flutter_dotenv: ^6.0.0
  razorpay_flutter: ^1.3.7
  equatable: ^2.0.5
  fpdart: ^1.1.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  